server.port=${PORT:8081}

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/warehousedb}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:ven@1047}
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=${SHOW_SQL:true}

jwt.secret=${JWT_SECRET:local-development-secret-key-change-before-deployment-1234567890}
spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${MAIL_PASSWORD:your-app-password}
app.frontend-url=${FRONTEND_URL:http://localhost:5173}
app.cors-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,http://127.0.0.1:5174}
app.seed-admin=${SEED_ADMIN:true}
app.seed-admin-email=${SEED_ADMIN_EMAIL:admin@warehouse.com}
app.seed-admin-password=${SEED_ADMIN_PASSWORD:Admin@123}